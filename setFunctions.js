const generateTitle = () => {
    var pageTitle = 'Node js'
    return pageTitle
}

const generateBody = () => {
    var pageBody = 'Hello world!!'
    return pageBody
}

console.log("loaded")

module.exports = {
    generateTitle,
    generateBody
}